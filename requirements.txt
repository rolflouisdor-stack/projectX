# Mailer Portal — production dependencies for DigitalOcean App Platform.
# Local dev uses bundled lib/ via PYTHONPATH; production installs these via
# pip on the App Platform builder. Versions pinned to what lib/ ships so
# behaviour is reproducible across environments.
Flask==3.1.3
SQLAlchemy==2.0.36
PyMySQL==1.1.2
python-dotenv==1.0.1
Werkzeug==3.1.7
PyJWT==2.12.1
openpyxl==3.1.5
gunicorn==25.3.0
boto3==1.35.71
redis==5.2.1
rq==2.1.0
stripe==11.6.0
